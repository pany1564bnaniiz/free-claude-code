IMPORTANT: Ensure you've thoroughly reviewed the [AGENTS.md](AGENTS.md) file before beginning any work.

## Personal Notes

- Forked for learning purposes and local experimentation
- Prefer running with `--verbose` flag during development for easier debugging
- Remember to update `.env` with your own API keys before running
- Use `python -m pytest tests/ -v` to run the test suite locally
- Keep a backup of working `.env` configs in a private gist (never commit them)
- Use `python -m pytest tests/ -v --tb=short` for cleaner failure output when running the full suite
- Alias frequently used command: `alias fcc='python -m free_claude_code'` for quicker CLI access
